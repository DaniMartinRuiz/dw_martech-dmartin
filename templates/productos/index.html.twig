{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-10 mx-auto">
                <div class="card bg-light">
                    <div class="card-header d-flex justify-content-between  bg-dark text-white">
                        <div>
                            <h5 class="">Producto</h5>
                        </div>
                        <div>
                            <a class="mr-0" href="{{ path('productos_new') }}">Crear un nuevo producto</a>
                        </div>
                    </div>

                    <div class="card-body">

                        <ul class="list-group">
                            {% for producto in productos %}
                                <div class="card list-group-item ">
                                    <div class="card-body">
                                        <ul class="list-group">
                                            <li class="list-group-item pb-2">
                                                <div class="lead"><h6 class="text-muted">Nombre</h6></div>
                                                <div class="lead">{{ producto.name }}</div >
                                            </li>
                                            <li class="list-group-item pb-2">
                                                <div class="lead"><h6 class="text-muted">Descripcion</h6></div>
                                                <div class="lead">{{ producto.description }}</div>
                                            </li>
                                            <li class="list-group-item pb-2">
                                                <div class="lead"><h6 class="text-muted">urlPicture</h6></div>
                                                <div class="lead">{{ producto.urlPicture }}</div>
                                            </li>
                                            <li class="list-group-item pb-2">
                                                <div class="lead"><h6 class="text-muted">Precio</h6></div>
                                                <div class="lead">{{ producto.price }}</div>
                                            </li>
                                           
                                            <li class="list-group-item pb-2 ml-auto border-left-0 border-right-0 border-bottom-0 ">
                                                <ul class="list-inline">
                                                    <li class="list-inline-item">
                                                        <a class="btn btn-outline-primary" href="{{ path('productos_show', { 'id': producto.id }) }}">
                                                            <span class="fa fa-info p-2"> </span>Mostrar
                                                        </a>
                                                    </li>
                                                    <li class="list-inline-item">
                                                        <a class="btn btn-outline-info" href="{{ path('productos_edit', { 'id': producto.id }) }}">
                                                            <span class="fa fa-edit p-2"> </span>Editar
                                                        </a>
                                                    </li>
                                                </ul>
                                            </li>
                                        </ul>
                                    </div>
                                </div>
                                <hr/>
                            {% endfor %}
                        </ul>

                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
